name: livekit-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - livekit-agents
      - livekit-plugins-silero
      - livekit-plugins-google
      - python-dotenv
      - llama-index
      - fastapi
      - pydantic
      - ruff
      - pytest
      - pytest-asyncio
      - httpx2
      - uvicorn
      - redis
      - aiomqtt
